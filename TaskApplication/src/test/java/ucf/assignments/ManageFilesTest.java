package ucf.assignments;

import org.junit.jupiter.api.RepeatedTest;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ManageFilesTest {

    @RepeatedTest(3)
    void importList()
    {
        // create a new String variable that holds the expected file name
        // create a new String variable and initialize it by calling the importList() function from the ManageFiles class
        // use assertEquals() to compare if the filenames(Strings) are the same
    }

    @RepeatedTest(3)
    void importMultipleLists()
    {
        // create a new String variable that holds the expected file name
        // create a new String variable and initialize it by calling the importMultipleLists() function from the ManageFiles class
        // use assertEquals() to compare if the filenames(Strings) are the same
    }

    @RepeatedTest(3)
    void exportList()
    {
        // create a String variable that is the expected name of the file
        // create a new String variable and initialize it by calling the exportList() function from the ManageFiles class
        // use assertEquals() to compare if the filenames(Strings) are the same
    }

    @RepeatedTest(3)
    void exportMultipleLists() {
        // create a String variable that is the expected name of the file
        // create a new String variable and initialize it by calling the exportMultipleLists() function from the ManageFiles class
        // use assertEquals() to compare if the filenames(Strings) are the same
    }
}